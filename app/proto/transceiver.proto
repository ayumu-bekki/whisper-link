syntax = "proto3";
package transceiver;

option go_package = "transceiver_test_client/proto";

service TransceiverService {
  // 双方向ストリーミング: クライアントから音声を送信し、サーバーからも音声を受信する
  rpc Connect(stream AudioChunk) returns (stream AudioChunk);
}

message AudioChunk {
  bytes ogg_opus_data = 1;
}
